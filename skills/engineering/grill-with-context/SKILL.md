---
name: grill-with-context
description: A relentless interview to sharpen a plan or design against the project's domain model, which also creates docs (ADRs and glossary) as we go.
disable-model-invocation: true
---

Run a `/grilling` session, using the `/domain-modeling` skill.
